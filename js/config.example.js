// Vorlage für die spätere, optionale Supabase-Anbindung.
// Diese Datei wird derzeit nicht von index.html geladen.
// Kopiere die Werte erst bei der Implementierung in eine passende Frontend-Konfiguration.
// Niemals einen service_role-Schlüssel im Browser verwenden.
window.BRAND_COLOR_BATTLE_SUPABASE = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
